// string -> is a collection of characters.

#include <stdio.h>

void main()
{
	char name[10];
	printf("\nEnter name :");
	//scanf("%s",name);
	gets(name);
	puts(name);
	//printf("\nname = %s", name);
}